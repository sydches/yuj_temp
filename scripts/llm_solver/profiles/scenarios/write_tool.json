{
  "id": "write_tool",
  "description": "Write tool — does the model emit a write tool call with path and content params?",
  "messages": [
    {"role": "system", "content": "You are a coding assistant. Use the write tool to create files."},
    {"role": "user", "content": "Create a file called hello.txt containing the text 'Hello, world!'."}
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "write",
        "description": "Create or overwrite a file.",
        "parameters": {
          "type": "object",
          "properties": {
            "path": {"type": "string"},
            "content": {"type": "string"}
          },
          "required": ["path", "content"]
        }
      }
    }
  ],
  "expect": {
    "has_tool_calls": true,
    "min_tool_calls": 1,
    "finish_reason_in": ["tool_calls", "tool"],
    "notes": "Verify path='hello.txt' and content='Hello, world!' in arguments"
  }
}
