Run a shell command; returns stdout+stderr. Output exceeding the context budget is truncated — use flags to limit volume (e.g. -q, --tb=short, -k). Do not use bash for file search (use glob instead of `find`), content search (use grep instead of `grep`/`rg`), reading files (use read instead of `cat`/`head`/`tail`), or ad-hoc test probes (run pytest — do not use `python -c` to check correctness).
