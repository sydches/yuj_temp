# Normalize rules — auto-generated by ProfileAnalyzer
# glm-4-flash, 2026-04-05
# Quirks detected: ['empty_content_on_tool_calls']

# GLM uses <think>...</think> for reasoning. The _base rules strip complete
# blocks, but GLM sometimes emits orphaned </think> tags (opening tag consumed
# by --reasoning-budget 0, closing tag leaks into content).
[[strip]]
name = "orphan_think_close"
pattern = '</think>'
target = "content"
replace = ""
